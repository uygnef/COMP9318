import helper

def pre_process(line):
    '''
    convert raw data to trainning format: (if_vowel, total_length, pre syllable.
    next syllable, position)
    :param line: 
    :return: 
    '''